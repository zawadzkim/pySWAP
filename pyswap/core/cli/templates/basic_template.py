import pyswap as ps

metadata = ps.Metadata(
    author="{author}",
    institution="{institution}",
    email="{email}",
    project="{project}",
    swap_ver="{swap_version}",
    comment="{comment}",
)
